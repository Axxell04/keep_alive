name: Ping Render Server

on:
  schedule:
    - cron: "*/26 * * * *"  # Cada 26 minutos

jobs:
  keep_alive:
    runs-on: ubuntu-latest
    steps:
      - name: Hacer petición al servidor
        run: curl -X GET "https://murci.onrender.com/keep_alive"
